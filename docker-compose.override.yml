version: '3.8'
services:
  rest-api:
    build:
      context: .
      dockerfile: Dockerfile.api
    container_name: kiki-rest-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped

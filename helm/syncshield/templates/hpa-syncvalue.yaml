{{- if .Values.syncvalue.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "syncshield.syncvalueFullname" . }}
  labels: {{ include "syncshield.labels" . | nindent 4 }}
    app.kubernetes.io/component: syncvalue
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "syncshield.syncvalueFullname" . }}
  minReplicas: {{ .Values.syncvalue.hpa.minReplicas }}
  maxReplicas: {{ .Values.syncvalue.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.syncvalue.hpa.targetCPUUtilizationPercentage }}
    {{- range .Values.syncvalue.hpa.customMetrics }}
    - {{ toYaml . | nindent 6 | trim }}
    {{- end }}
{{- end }}
